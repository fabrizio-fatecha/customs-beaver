version: '3.8'

services:
  nest-app:
    build: .
    container_name: nest
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway" 
    environment:
      - NODE_ENV=production